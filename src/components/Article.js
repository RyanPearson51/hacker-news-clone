/*import React, {useState} from 'react'
export default function Article(props) {
    return (
        <div className="story-container" key={props.article.objectID}>
          <h4 style={{margin: "5px"}}>{props.article.title}</h4>
          <p style={{fontSize: "20px", margin: "5px"}}>Author: {props.article.author}</p>
          <a href={props.article.url} target="_blank" rel="noreferrer" style={{color: "white", fontSize: "18px"}}>Visit Website</a>
        </div>
      )}
   */
    